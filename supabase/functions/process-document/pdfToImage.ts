
// Cette fonction sera implémentée pour utiliser l'API Google Cloud Vision pour la conversion
export async function processImage(pdfFile: Blob, pageNumber: number, documentId: string): Promise<Uint8Array | null> {
  // À implémenter : logique de conversion PDF vers image
  // Pour l'instant, retourne null pour indiquer que la fonction n'est pas encore implémentée
  return null
}
